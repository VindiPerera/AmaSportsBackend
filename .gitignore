*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot

# Mobile app web export. `npm run deploy:web` (see scripts/deploy-web.mjs)
# copies sport-mobile's built `dist/` into public/ below — same idea as
# /public/build above, just for the Expo Router bundle instead of the
# admin panel's. /mobile-dist is where you drop that `dist/` folder's
# contents when sport-mobile isn't checked out alongside this repo.
/storage/*.key
/storage/pail
/storage/app/private/firebase-service-account.json
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
